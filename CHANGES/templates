Vanilla arc 3.1 templates can have sometimes drop data when writing to and
then reading from disk. Anarki avoids losing data, which also requires some
other incompatibilities around nil handling. See `(help deftem)` for details.
